# FindIt Frontend

React + TypeScript + Vite frontend for FindIt.

This project is currently organized from the Google AI Studio UI prototype under `docs/ui-reference/`. It intentionally keeps mock data and does not call the backend API yet.

## Local Development

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:3000`.
